# This program uses a Ruby method to sort large quantities of data in either ascending or descending order.

def alphabetize(arr,rev=false)
    arr.sort!
    if rev
        arr.reverse!
    else
        arr
    end
end

numbers = [5,1,3,8]
puts "#{alphabetize(numbers)}"
